{
  "name": "vis.csail.mit.edu",
  "version": "1.0.0",
  "homepage": "https://vis.csail.mit.edu",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mitvis/website.git"
  },
  "scripts": {
    "start": "npm run build && node scripts/watch.js & reload -p 5000 -d build",
    "prebuild": "rm -Rf build && mkdir build",
    "build": "node scripts/build.js",
    "deploy": "scripts/deploy.sh"
  },
  "devDependencies": {
    "chalk": "^1.1.3",
    "d3-time-format": "^2.0.2",
    "fs-extra": "^8.1.0",
    "handlebars": "^4.0.5",
    "handlebars-helpers": "^0.7.5",
    "node-sass": "^6.0.1",
    "node-watch": "^0.4.0",
    "reload": "^3.1.0",
    "require-reload": "^0.2.2"
  }
}
